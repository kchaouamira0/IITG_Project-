{% extends 'BaseAdmin.html.twig' %}

{% block title %}Domaine{% endblock %}

{% block body %}
    <h1>Domaine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ domaine.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ domaine.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ domaine.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_iitg_admin_domaine_index') }}">back to list</a>

    <a href="{{ path('app_iitg_admin_domaine_edit', {'id': domaine.id}) }}">edit</a>

    {{ include('iitg_admin/domaine/_delete_form.html.twig') }}
{% endblock %}
