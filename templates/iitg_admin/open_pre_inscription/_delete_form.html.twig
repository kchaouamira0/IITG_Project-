<form method="post" action="{{ path('app_iitg_admin_open_pre_inscription_delete', {'id': open_pre_inscription.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ open_pre_inscription.id) }}">
    <button class="btn btn-primary">Delete</button>
</form>
