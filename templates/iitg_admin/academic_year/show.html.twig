{% extends 'BaseAdmin.html.twig' %}

{% block title %}AcademicYear{% endblock %}

{% block body %}
    <h1>AcademicYear</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ academic_year.id }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ academic_year.value }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ academic_year.name }}</td>
            </tr>
            <tr>
                <th>Current</th>
                <td>{{ academic_year.current ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_iitg_admin_academic_year_index') }}">back to list</a>

    <a href="{{ path('app_iitg_admin_academic_year_edit', {'id': academic_year.id}) }}">edit</a>

    {{ include('iitg_admin/academic_year/_delete_form.html.twig') }}
{% endblock %}
