{% extends 'BaseAdmin.html.twig' %}

{% block title %}FiliereByParcours{% endblock %}

{% block body %}
    <h1>FiliereByParcours</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ filiere_by_parcour.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ filiere_by_parcour.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_iitg_admin_filiere_by_parcours_index') }}">back to list</a>

    <a href="{{ path('app_iitg_admin_filiere_by_parcours_edit', {'id': filiere_by_parcour.id}) }}">edit</a>

    {{ include('iitg_admin/filiere_by_parcours/_delete_form.html.twig') }}
{% endblock %}
