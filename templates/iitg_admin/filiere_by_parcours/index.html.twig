{% extends 'BaseAdmin.html.twig' %}

{% block title %}FiliereByParcours index{% endblock %}

{% block body %}
    <h1>FiliereByParcours index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Parcours</th>
                <th>Filiere</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for filiere_by_parcour in filiere_by_parcours %}
            <tr>
                <td>{{ filiere_by_parcour.id }}</td>
                <td>{{ filiere_by_parcour.parcours }}</td>
                <td>{{ filiere_by_parcour.filiere }}</td>
                <td>
                    <a href="{{ path('app_iitg_admin_filiere_by_parcours_show', {'id': filiere_by_parcour.id}) }}">show</a>
                    <a href="{{ path('app_iitg_admin_filiere_by_parcours_edit', {'id': filiere_by_parcour.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_iitg_admin_filiere_by_parcours_new') }}">Create new</a>
{% endblock %}
