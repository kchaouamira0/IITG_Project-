{% extends 'base.html.twig' %}

{% block title %}Phone{% endblock %}

{% block body %}
    <h1>Phone</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ phone.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ phone.number }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_iitg_admin_phone_index') }}">back to list</a>

    <a href="{{ path('app_iitg_admin_phone_edit', {'id': phone.id}) }}">edit</a>

    {{ include('iitg_admin/phone/_delete_form.html.twig') }}
{% endblock %}
