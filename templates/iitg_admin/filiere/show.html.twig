{% extends 'BaseAdmin.html.twig' %}

{% block title %}Filiere{% endblock %}

{% block body %}
    <h1>Filiere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ filiere.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ filiere.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ filiere.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_iitg_admin_filiere_index') }}">back to list</a>

    <a href="{{ path('app_iitg_admin_filiere_edit', {'id': filiere.id}) }}">edit</a>

    {{ include('iitg_admin/filiere/_delete_form.html.twig') }}
{% endblock %}
