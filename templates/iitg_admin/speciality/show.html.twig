{% extends 'BaseAdmin.html.twig' %}

{% block title %}Speciality{% endblock %}

{% block body %}
    <h1>Speciality</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ speciality.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ speciality.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ speciality.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_iitg_admin_speciality_index') }}">back to list</a>

    <a href="{{ path('app_iitg_admin_speciality_edit', {'id': speciality.id}) }}">edit</a>

    {{ include('iitg_admin/speciality/_delete_form.html.twig') }}
{% endblock %}
